---
import Icon from './Icon.astro';

interface Props {
  activeHref?: string;
}

const { activeHref = '#' } = Astro.props;

const links = [
  { href: '#contest', label: 'The Contest' },
  { href: '#preparation', label: 'Preparation' },
  { href: '#calendar', label: 'Calendar' },
  { href: '#rules', label: 'Rules' },
  { href: '#hall-of-fame', label: 'Hall of Fame' },
];
---
<nav class="fixed top-0 left-0 w-full z-50 bg-surface-container backdrop-blur-sm border-b-2 border-outline-variant">
  <div class="flex justify-between items-center w-full px-margin py-sm max-w-7xl mx-auto">
    <a href="/en/" class="flex items-center gap-sm shrink-0">
      <img
        src="/img/logo-beoi-white.png"
        alt="beOI"
        class="h-10 w-10 object-contain"
        width="40"
        height="40"
      />
      <span class="font-headline text-headline-md text-footer-text tracking-tight hidden sm:inline">beOI</span>
    </a>

    <div class="hidden md:flex items-center gap-gutter">
      {links.map((link) => (
        <a
          href={link.href}
          class:list={[
            'font-label text-label-md transition-colors',
            link.href === activeHref
              ? 'text-footer-text border-b-2 border-footer-text pb-1'
              : 'text-footer-text hover:underline',
          ]}
        >
          {link.label}
        </a>
      ))}
    </div>

    <div class="flex items-center gap-sm">
      <button
        type="button"
        class="text-footer-text hover:underline text-2xl leading-none"
        aria-label="Change language"
      >
        <Icon name="language" />
      </button>
    </div>
  </div>
</nav>
